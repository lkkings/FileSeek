## 1. 项目骨架与依赖

- [x] 1.1 建立仓库结构（`backend/`、`desktop/`、`extensions/{windows,macos,linux}/`、`tests/`）并验证目录与占位 README 均已生成
- [x] 1.2 配置 Python 后端工程（依赖管理、`ruff`、`mypy`、`pytest` + `pytest-cov`），验证 `pytest` 在空测试集下退出码为 0 且 lint/type check 通过
- [x] 1.3 固定后端核心依赖版本（fastapi、uvicorn、faiss-cpu、torch、transformers、sentence-transformers、rapidocr-onnxruntime、av、zeroconf、keyring），验证在干净虚拟环境中安装成功且 `import faiss, torch, av` 无错
- [x] 1.4 配置覆盖率门限为 80% 并接入 CI 脚本，验证覆盖率不足时命令以非零码失败
- [x] 1.5 建立用户配置目录解析（三平台各自的配置/缓存/数据目录）与 `AppPaths` 单元测试，验证三平台分支均被测试覆盖

## 2. 元数据存储层

- [x] 2.1 编写 SQLite schema 迁移脚本，创建 `files`、`documents`、`images`、`videos`、`video_segments`、`doc_chunks`、`tasks`、`schema_meta` 表及索引，验证迁移在空库上执行后 `PRAGMA table_info` 返回预期列
- [x] 2.2 在 `files.content_hash` 上建唯一索引，验证插入重复摘要的第二条记录被数据库拒绝的测试通过
- [x] 2.3 建立 FTS5 虚表覆盖文件名与文档文本，验证按关键词查询能命中对应记录的测试通过
- [x] 2.4 实现 schema 版本化与启动时迁移检查（`schema_meta`），验证从旧版本号升级与版本号一致时跳过迁移两个用例均通过
- [x] 2.5 实现 `FileRepository`（增删查、按摘要查、按 id 批量查、更新 `index_state`），验证其单元测试覆盖全部方法
- [x] 2.6 实现 `SegmentRepository` 与 `ChunkRepository`，验证按父文件查询片段/分块且按起始时间与 chunk_index 有序的测试通过
- [x] 2.7 实现拒绝把元数据库或索引路径指向网络路径的校验，验证传入 UNC/挂载路径时抛出配置错误的测试通过

## 3. 文件搬运与去重

- [x] 3.1 实现流式 SHA-256 计算，验证对已知内容的摘要与参考值一致的测试通过
- [x] 3.2 实现同卷 `rename` 搬运路径，验证搬运后源路径不存在、目标路径存在的测试通过
- [x] 3.3 实现跨卷「复制-校验-删除」搬运：临时后缀写入、fsync、摘要与大小校验、原子改名、最后删源，验证成功路径与「校验失败则保留源文件并清理临时文件」两个测试通过
- [x] 3.4 实现搬运中断的清理逻辑（启动时清理孤儿 `.fileseek-part` 文件），验证残留临时文件在启动后被移除的测试通过
- [x] 3.5 实现库内命名冲突消解（同名不同内容时生成不冲突名称），验证冲突时不覆盖已有文件的测试通过
- [x] 3.6 实现库目录布局初始化（文档/图片/视频/缩略图子目录自动创建）与不可写目录拒绝，验证两个测试通过
- [x] 3.7 实现去重逻辑：摘要命中已有记录时不产生副本并把新原始路径追加到来源列表，验证重复入库后库内仅一份文件且来源列表含两条路径的测试通过
- [x] 3.8 实现搬运失败时保留源文件并写入任务失败原因，验证目标不可写场景下源文件完好的测试通过

## 4. 任务队列

- [x] 4.1 实现任务记录模型与状态机（queued/processing/completed/failed）及非法状态转移拒绝，验证状态机单元测试通过
- [x] 4.2 实现按 `priority DESC, created_at ASC` 的事务化取任务，验证并发取任务不会把同一任务分配两次的测试通过
- [x] 4.3 实现并发上限控制（可配置，默认 3），验证提交 10 个任务时同时处于 processing 的数量不超过上限的测试通过
- [x] 4.4 实现优先级区分（右键单次提交为高优先级，批量导入为低优先级），验证批量任务排队期间新提交的高优先级任务被优先取出的测试通过
- [x] 4.5 实现阶段级断点（`completed_stages` 记录 moved/extracted/encoded/indexed 及中间产物位置），验证重试跳过已完成阶段的测试通过
- [x] 4.6 实现服务重启恢复（残留 processing 重置为 queued 且保留 completed_stages），验证重启后任务继续且不重复已完成阶段的测试通过
- [x] 4.7 实现进度上报接口（阶段 + 0..100 单调递增进度），验证进度序列单调不减的测试通过
- [x] 4.8 实现失败任务重试入口（无需重新选择文件），验证对失败任务调用重试后状态回到 queued 的测试通过

## 5. 模型工作进程与硬件自适应

- [x] 5.1 实现硬件探测（CUDA 可用性与显存、Apple MPS、回退 CPU）并输出档位决策，验证三种模拟环境下档位选择正确的测试通过
- [x] 5.2 实现模型档位配置与用户覆盖（强制 GPU / 强制 CPU / 指定规模），验证覆盖优先于自动探测的测试通过
- [x] 5.3 实现模型权重存放与就位检查，验证权重缺失时报告「索引不可用」状态的测试通过
- [x] 5.4 实现权重下载器（需用户同意、支持断点续传、校验摘要）与离线权重包手动放置路径，验证摘要不符时拒绝启用该权重的测试通过
- [x] 5.5 实现独立模型工作进程与父进程间的请求/响应队列协议，验证进程启动后能完成一次编码往返的集成测试通过
- [x] 5.6 在工作进程中经 `transformers.ChineseCLIPModel` 加载 Chinese-CLIP（图像+文本双塔）并实现批量图像编码与文本编码，验证同一语义的中文文本与匹配图像相似度高于不匹配图像的测试通过
- [x] 5.7 在工作进程中加载多语言文本编码模型，验证中英同义句向量相似度高于无关句的测试通过
- [x] 5.8 接入 RapidOCR 并实现图像文字识别，验证含中文文字的测试图片能识别出预期关键词的测试通过
- [x] 5.9 实现显存不足时自动降级到 CPU 档并告知用户，验证模拟 OOM 时降级成功且不抛出未捕获异常的测试通过
- [x] 5.10 实现向量 L2 归一化与批大小随档位调整，验证归一化后模长为 1 的测试通过
- [x] 5.11 暴露当前生效模型与计算设备的查询接口，验证返回值与实际加载档位一致的测试通过

## 6. 向量索引层

- [x] 6.1 实现基于 `IndexIDMap2` + `IndexFlatIP` 的索引封装（add/search/remove_ids/持久化/加载），验证增删查与重载后结果一致的测试通过
- [x] 6.2 建立三个独立索引（文档分块、图片、视频片段）及其稳定整型 id 分配，验证 id 在重启后仍能解析回同一元数据记录的测试通过
- [x] 6.3 在索引旁记录产生它的模型标识与向量维度（写入 `schema_meta`），验证记录随索引持久化的测试通过
- [x] 6.4 实现加载时的模型兼容校验：标识不符则拒绝混写并返回需重建状态，验证切换档位后写入被拒绝的测试通过
- [x] 6.5 实现从元数据与库内文件重建索引的入口，验证删除索引文件后重建能恢复检索结果的集成测试通过
- [x] 6.6 实现索引损坏检测与用户可见的重建提示，验证损坏索引文件在启动时被识别且不静默丢弃已入库文件的测试通过
- [x] 6.7 实现删除策略（标记 `index_state=missing` 时检索层过滤，批量物化删除单独执行），验证被标记文件不出现在检索结果的测试通过

## 7. 图片索引管线

- [x] 7.1 实现图片解码与元数据抽取（宽高），验证对多种格式取得正确尺寸的测试通过
- [x] 7.2 实现图片入库管线（搬运 → 编码 → OCR → 写元数据 → 写索引），验证入库一张图片后可被检索命中的集成测试通过
- [x] 7.3 把 OCR 文本写入 `images.ocr_text` 并纳入 FTS，验证按图中文字检索命中该图片的测试通过
- [x] 7.4 实现不可解码图片的失败处理，验证损坏图片使任务失败并附原因且文件仍在库中的测试通过

## 8. 文档索引管线

- [x] 8.1 实现文档文本抽取（PDF、DOCX、TXT、MD）与页数统计，验证各格式抽取出预期文本片段的测试通过
- [x] 8.2 实现文本分块（记录 chunk_index 与字符区间），验证长文档被完整分块且区间连续无遗漏的测试通过
- [ ] 8.3 实现文档入库管线（搬运 → 抽取 → 分块 → 编码 → 写元数据/FTS/索引），验证按主题的自然语言查询命中该文档的集成测试通过
- [ ] 8.4 实现无文本层 PDF 的 OCR 回退，验证扫描版 PDF 经 OCR 后可被内容检索命中、仍无文本时被标记为仅可按文件名检索的两个测试通过
- [ ] 8.5 实现加密或损坏文档的失败处理，验证任务失败并附原因、文件保留在库中的测试通过

## 9. 视频索引管线

- [ ] 9.1 实现视频容器探测与元数据抽取（时长、宽高、帧率），验证对多种容器取得正确元数据的测试通过
- [ ] 9.2 实现按固定采样率（默认 1 fps，可配置）抽帧，验证对已知时长视频抽出预期帧数的测试通过
- [ ] 9.3 实现帧批量编码（批大小随档位调整），验证批量与逐帧编码结果一致的测试通过
- [ ] 9.4 实现基于相邻帧向量相似度的片段切分，含最短 2s / 最长 30s 约束，验证内容突变处产生边界且片段时长落在约束内的测试通过
- [ ] 9.5 实现片段向量聚合（段内均值 + 归一化），验证聚合向量模长为 1 且与段内帧向量相似度高的测试通过
- [ ] 9.6 实现片段关键帧缩略图生成并写入库内 `.thumbnails/`，验证每个片段都产出可读缩略图文件的测试通过
- [ ] 9.7 实现缩略图本地缓存的 write-through 与按片段标识读取接口，验证读取不触碰原视频文件的测试通过
- [ ] 9.8 实现视频入库管线整体串联并写入 `videos` / `video_segments` / 索引，验证入库视频后按片段内容检索返回带起止时间的片段的集成测试通过
- [ ] 9.9 实现不可解码视频的失败处理，验证任务失败并附原因的测试通过
- [ ] 9.10 实现视频索引的阶段级进度上报（搬运/抽帧/编码/索引），验证各阶段进度可观察且单调递增的测试通过

## 10. 检索 API

- [ ] 10.1 实现查询文本编码与空查询拒绝，验证空白查询返回明确提示且不执行检索的测试通过
- [ ] 10.2 实现按类型分组的检索与相关度降序排序，验证三类命中被正确分组且组内有序的测试通过
- [ ] 10.3 实现类型限定检索，验证限定为视频时结果不含文档与图片的测试通过
- [ ] 10.4 实现文档结果的分块聚合（按文件取最高分代表）与匹配文本片段返回，验证结果含来自该文档的匹配文本的测试通过
- [ ] 10.5 实现视频结果按视频聚合、片段按起始时间升序并携带起止时间/相关度/缩略图引用，验证单视频三处命中返回一个条目下三个有序片段的测试通过
- [ ] 10.6 实现结果数量上限与获取更多结果的分页语义，验证数千命中时首批数量受限且可取后续结果的测试通过
- [ ] 10.7 实现向量检索与 FTS 关键词通道的结果合并，验证仅字面匹配的文件也能被命中的测试通过
- [ ] 10.8 实现缺失/不可访问文件的结果标注，验证被标记 missing 的命中带不可访问标识的测试通过
- [ ] 10.9 实现索引进行中的检索可用性，验证有任务处于 processing 时检索仍返回已完成内容的测试通过
- [ ] 10.10 建立万级规模检索性能测试，验证一万条向量下检索在 2 秒内返回

## 11. 后端服务与本地 IPC 安全

- [ ] 11.1 实现 FastAPI 应用装配与统一响应信封（状态、数据、错误消息、分页元数据），验证成功与失败响应结构的测试通过
- [ ] 11.2 实现仅绑定 `127.0.0.1` 与端口探测，验证服务不监听非回环地址的测试通过
- [ ] 11.3 实现 `{port, token}` 连接信息文件写入（仅当前用户可读）与启动时 token 轮换，验证文件权限与轮换行为的测试通过
- [ ] 11.4 实现 token 鉴权中间件与请求来源校验（`Origin` / `Sec-Fetch-Site`），验证无 token、错误 token、跨站来源三种请求均被拒绝的测试通过
- [ ] 11.5 实现 `POST /ingest`（接受路径列表、过滤不支持类型、返回任务 id、不阻塞），验证混合类型提交仅为受支持文件建任务且响应在 500ms 内返回的测试通过
- [ ] 11.6 实现任务查询与重试端点，验证进度查询与重试触发的集成测试通过
- [ ] 11.7 实现检索端点，验证按查询返回分组结果的集成测试通过
- [ ] 11.8 实现缩略图读取端点，验证按片段标识返回图像字节的测试通过
- [ ] 11.9 实现 `GET /media/{file_id}` 流式端点（支持 HTTP Range、路径限制在库根目录内），验证 Range 请求返回 206 与部分字节、越界路径被拒绝的两个测试通过
- [ ] 11.10 实现所有端点的输入校验（路径、id、分页参数）与错误消息不泄露凭据，验证非法输入被拒绝且错误消息不含密码的测试通过
- [ ] 11.11 实现端点速率限制，验证超过阈值的请求被限流的测试通过
- [ ] 11.12 实现健康检查端点与优雅关闭（停止取新任务、保存索引），验证关闭后索引文件完整可加载的测试通过

## 12. 桌面客户端骨架

- [ ] 12.1 初始化 Tauri v2 + React + TypeScript 工程并接入 lint/type check，验证 `tsc --noEmit` 与开发构建均成功
- [ ] 12.2 实现 Tauri sidecar 管理后端进程（启动、就绪探测、退出时停止），验证应用启动后健康检查通过、退出后后端进程结束的测试通过
- [ ] 12.3 实现前端读取连接信息文件并在请求中携带 token 的 API 客户端，验证鉴权请求成功的集成测试通过
- [ ] 12.4 实现系统托盘（打开主界面、显示处理中任务数、退出），验证托盘菜单项行为的测试通过
- [ ] 12.5 实现关闭主窗口保留托盘且后端继续运行、从托盘退出则停止后端，验证两条行为路径的测试通过
- [ ] 12.6 实现开机自启开关（三平台各自注册方式），验证开关状态与系统注册项一致的测试通过
- [ ] 12.7 建立设计令牌（颜色、字号、间距、动效时长）与基础样式层，验证令牌在深浅两套主题下均有定义

## 13. 检索 UI 与结果呈现

- [ ] 13.1 实现搜索输入与类型筛选控件（含 hover/focus/active 状态），验证键盘操作可完成查询提交的测试通过
- [ ] 13.2 实现按类型分组的结果布局，验证三组结果按设计层级渲染的组件测试通过
- [ ] 13.3 实现图片结果缩略图网格（显式尺寸、懒加载），验证无布局抖动且下方图片延迟加载的测试通过
- [ ] 13.4 实现视频结果条目与其下片段缩略图列表（含起止时间与相关度），验证三个片段按时间升序渲染的测试通过
- [ ] 13.5 实现文档结果条目与匹配文本片段展示，验证匹配文本可见的测试通过
- [ ] 13.6 实现空结果与不可访问结果的呈现，验证空结果显示说明文案、不可访问结果带可辨识标识的测试通过
- [ ] 13.7 实现分页/加载更多交互，验证获取后续结果后列表追加的测试通过
- [ ] 13.8 实现「在文件管理器中显示」动作，验证调用后打开该文件所在目录的测试通过
- [ ] 13.9 实现可访问性要求（语义化标签、键盘导航、对比度、reduced-motion），验证自动化可访问性检查无违规

## 14. 视频播放与片段跳转

- [ ] 14.1 实现内嵌播放器组件对接 `GET /media/{file_id}`，验证播放器能加载并播放库内视频的测试通过
- [ ] 14.2 实现点击片段缩略图从该片段起始时间开始播放，验证点击 2:15 的片段后播放位置为 135 秒的测试通过
- [ ] 14.3 实现播放失败的用户提示（文件不可访问、解码失败），验证失败时显示原因而非静默无响应的测试通过
- [ ] 14.4 实现片段时间轴导航（在同一视频的多个命中片段间切换），验证切换后播放位置更新的测试通过
- [ ] 14.5 提供「用系统默认程序打开」次要入口，验证调用后系统播放器被启动的测试通过

## 15. 任务视图、设置与首次启动

- [ ] 15.1 实现任务状态视图（状态、阶段、进度、失败原因、重试按钮），验证失败任务展示原因且重试入口可用的测试通过
- [ ] 15.2 实现任务进度的自动更新（无需手动刷新），验证进度在后端推进后于界面更新的测试通过
- [ ] 15.3 实现设置界面的库根目录配置与可写校验，验证选择不可写目录时保留原配置并说明原因的测试通过
- [ ] 15.4 实现设置界面展示当前生效模型与计算设备，验证显示值与后端查询结果一致的测试通过
- [ ] 15.5 实现并发上限与视频采样率的设置项，验证修改后新任务遵循新配置的测试通过
- [ ] 15.6 实现模型档位偏好设置与「切换需重建索引」的确认流程，验证用户确认前不混写新向量的测试通过
- [ ] 15.7 实现首次启动引导（配置库根目录、展示探测到的硬件与将使用的模型），验证未配置库目录时引导出现的测试通过
- [ ] 15.8 实现权重下载同意与下载进度展示，并在权重就位前明示索引不可用，验证权重缺失时界面不声称索引可用的测试通过

## 16. 入库入口：CLI 与三平台右键扩展

- [ ] 16.1 实现 `fileseek add <paths...>` CLI（读取连接信息、必要时启动后端、提交任务），验证命令对一批文件提交成功并输出任务 id
- [ ] 16.2 实现 UI 拖拽入库区域，验证拖入文件后生成对应任务的测试通过
- [x] 16.3 实现受支持文件类型判定的共享清单（供扩展与后端一致使用），验证清单单元测试覆盖三类扩展名
- [ ] 16.4 实现 Windows 右键扩展（Rust + windows-rs，先用 `IContextMenu` 打通），验证在资源管理器中对受支持文件出现菜单项且点击后任务被创建
- [ ] 16.5 实现 Windows 扩展的后端未运行时自动启动与启动超时的可读失败提示，验证两条路径的行为
- [ ] 16.6 升级 Windows 扩展为 `IExplorerCommand` 并通过 sparse MSIX package 注册，验证菜单项出现在 Windows 11 一级右键菜单中
- [ ] 16.7 实现 macOS Finder Extension（`FIFinderSync`）并打入 app bundle，验证在 Finder 中对受支持文件出现菜单项且点击后任务被创建
- [ ] 16.8 完成 macOS 应用签名与公证流程脚本，验证公证后的安装包在干净系统上可安装且扩展可启用
- [ ] 16.9 实现 Linux Nautilus 扩展（Python GObject，调用共享 CLI），验证在 Nautilus 中菜单项可见且入库成功
- [ ] 16.10 实现 Linux Dolphin service menu 与 Thunar custom action，验证两者菜单项均可触发入库
- [ ] 16.11 验证三平台扩展的不受支持文件与多选混合类型行为：菜单在无受支持文件时不显示，混合选择仅为受支持文件建任务
- [ ] 16.12 验证三平台扩展均在 500ms 内交回控制权（含提交 5GB 视频场景），文件管理器不出现卡顿

## 17. NAS 接入（模式 B）

- [ ] 17.1 实现 NAS 手动配置（协议、主机、共享路径、用户名）与连接测试，验证连接可用与失败原因可读两个测试通过
- [ ] 17.2 实现凭据经系统密钥链存储（配置文件仅存引用），验证配置文件不含明文密码且日志与错误消息不输出凭据的测试通过
- [ ] 17.3 实现 mDNS 自动发现（可关闭）与发现结果列表，验证发现到广播主机、超时无结果两种场景的测试通过
- [ ] 17.4 实现 NAS 模式下的存储位置划分（文件与缩略图在 NAS，索引与元数据强制本地），验证索引与元数据未写入 NAS 路径的测试通过
- [ ] 17.5 实现周期性连接健康检查与断连通知，验证库根目录不可达时产生用户可见状态的测试通过
- [ ] 17.6 实现断连时暂停需写库目录的任务（置为 queued 而非 failed）与恢复后自动继续，验证暂停与恢复两个测试通过
- [ ] 17.7 验证 NAS 不可达时检索仍基于本地索引返回结果且结果被标注不可访问
- [ ] 17.8 验证 NAS 不可达时已本地缓存的缩略图仍可展示
- [ ] 17.9 实现设置界面的 NAS 连接状态展示与发现/手动配置入口，验证断连状态在界面呈现的测试通过

## 18. 库一致性与外部变更

- [x] 18.1 实现库内文件一致性检查（存在性 + 摘要比对），验证扫描能识别缺失与被替换文件的测试通过
- [ ] 18.2 实现缺失文件标记为 missing 且不再作为可访问结果返回，验证检索结果标注的测试通过
- [ ] 18.3 实现被替换文件标记为 needs_reindex 并可触发重新索引，验证重新索引后检索命中新内容的测试通过
- [ ] 18.4 实现一致性检查的周期性调度（可配置、低优先级），验证调度不与入库任务争抢并发额度的测试通过

## 19. 端到端验证与打包

- [ ] 19.1 建立 E2E 测试环境（启动后端 + 客户端，使用固定样例文件集），验证测试可重复运行且不依赖外部网络
- [ ] 19.2 E2E：右键入库一张图片后用中文自然语言查询命中该图片，验证流程通过
- [ ] 19.3 E2E：入库一篇中文 PDF 后按主题查询命中并展示匹配文本，验证流程通过
- [ ] 19.4 E2E：入库一个视频后按画面内容查询命中片段，点击缩略图跳转到对应时间播放，验证流程通过
- [ ] 19.5 E2E：批量导入期间提交单个文件，验证其被优先处理且检索在索引进行中可用
- [ ] 19.6 E2E：NAS 断连与恢复场景，验证任务暂停、检索降级标注、恢复后继续三项行为
- [ ] 19.7 视觉回归测试覆盖 320/768/1024/1440 宽度下的搜索页与结果页（含深浅主题），验证截图基线建立且无溢出
- [ ] 19.8 运行覆盖率检查，验证后端整体覆盖率达到 80% 以上
- [ ] 19.9 建立三平台打包脚本（Python 后端打包 + Tauri 构建 + 扩展产物），验证各平台产出可安装包
- [ ] 19.10 记录并检查安装包体积预算与首次启动权重下载体积，验证实际值写入发布检查清单
- [ ] 19.11 在干净的三平台环境执行安装到检索的冒烟验证，验证首次启动引导、权重下载、入库、检索全流程可用
